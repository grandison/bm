<% if @with_order %>
  window.location = '<%= url_for(@order) %>';
<% else %>
  <% if @count %>
    $("#emails-count").text("<%= @count %> емейлов найдено.");
  <% else %>
  setTimeout(function(){
    $("#new_order").submit();
  }, 10000);
  <% end %>
<% end %>